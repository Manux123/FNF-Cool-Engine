package states;
class GJKeys
{
    public static var id:Int = ; // since the engine still isnt on game jolt idk the id
    public static var key:String = ""; // secret api key but same with the up one idk cuz no gamejolt yet
}
